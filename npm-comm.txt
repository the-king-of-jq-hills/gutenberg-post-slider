E:\xampp8\htdocs\wp\wp-content\plugins>npx @wordpress/create-block txp-slider

Creating a new WordPress plugin in the txp-slider directory.

Creating a "block.json" file.

Creating a "package.json" file.

Installing `@wordpress/scripts` package. It might take a couple of minutes...

Formatting JavaScript files.

Compiling block.

Done: WordPress plugin Txp Slider bootstrapped in the txp-slider directory.

You can run several commands inside:

  $ npm start
    Starts the build for development.

  $ npm run build
    Builds the code for production.

  $ npm run format
    Formats files.

  $ npm run lint:css
    Lints CSS files.

  $ npm run lint:js
    Lints JavaScript files.

  $ npm run plugin-zip
    Creates a zip file for a WordPress plugin.

  $ npm run packages-update
    Updates WordPress packages to the latest version.

To enter the directory type:

  $ cd txp-slider

You can start development with:

  $ npm start

Code is Poetry

E:\xampp8\htdocs\wp\wp-content\plugins>